# Contributing

1. Fork and clone the repo
2. `mix deps.get`
3. `mix test` (unit tests)
4. `just all-tests` (unit + integration; starts local Postgres)

Format before opening a PR:

```sh
mix format
```
